# Environment variables for CodeLens AI
# Copy this file to .env (never commit your real secrets) and fill in values.

# Google Gemini API Key (Required for analysis)
VITE_GEMINI_API_KEY=YOUR_GEMINI_KEY_HERE

# Optional: GitHub Personal Access Token to raise rate limits
# Generate at https://github.com/settings/tokens (no scopes needed for public repo read)
VITE_GITHUB_TOKEN=
